# Auditoría de Ingeniería — General + Feature Negocios

**Fecha:** 2026-08-04
**Proyecto:** gestion_inventario
**Rama:** develop
**Score:** 2.58 / 5.00 🔴
**Responsable:** Claude Code (auditoria-ingenieria)

---

## 1. Contexto y método

**Alcance:** Proyecto completo + nueva funcionalidad de gestión de negocios (ui_businesses.py, tab_businesses.html, business_edit_form.html)
**Archivos auditados:** 38 Python + 57 HTML en app/
**Criterios:** 10 dimensiones según plantilla de auditoría de ingeniería
**Limitaciones:** No se evaluó infraestructura Docker ni Caddy en profundidad

---

## 2. Checklist por dimensión

| # | Dimensión | Peso | Veredicto | Score | Evidencia |
|---|-----------|------|-----------|-------|-----------|
| 1 | SoC / SRP | 10% | ⚠️ | 2.5 | ui_tabs.py 2417 líneas, inventory_service.py 3069 líneas mezclando >10 responsabilidades |
| 2 | Cohesión / Acoplamiento | 10% | ⚠️ | 3.0 | HTTPException en servicios, debug prints en security.py, patrones duplicados en repos |
| 3 | Capas / Límites | 10% | ⚠️ | 2.5 | 16 queries DB directas en ui_tabs.py; routers sin capa de servicio |
| 4 | Seguridad | 15% | ❌ | 1.5 | CRÍTICO: BD+cookies en git; sin CSRF; /docs público; auth gaps en sales/expenses; default creds admin/admin; HTTPS off |
| 5 | Robustez / Resiliencia | 15% | ❌ | 2.0 | ~100+ except Exception; TOCTOU en business_create; FIFO race; audit silencioso; health check ciego |
| 6 | Consistencia transaccional | 10% | ❌ | 2.0 | CRÍTICO: FKs no enforcement en SQLite → business_delete huérfano; migrations sin FK; Location unique incorrecto |
| 7 | Observabilidad | 10% | ❌ | 1.5 | 8 print() debug en prod; health check superficial sin DB; sin structured logging |
| 8 | Configuración / Entornos | 5% | ⚠️ | 3.5 | Mayoría vía env vars; sin CI/CD; business_config.conf con path hardcodeado |
| 9 | Pruebas / Calidad | 10% | ⚠️ | 3.0 | 50 tests (1256 líneas) para 14,808 líneas de código (~8.5%); sin tests del nuevo feature |
| 10 | Escalabilidad | 5% | ✅ | 4.0 | Stateless (cookie sessions); connection pooling; algunos queries sin límite |

**Leyenda:** ✅ OK (4-5) · ⚠️ PARCIAL (2-3) · ❌ NO (<2)

---

## 3. Scorecard ponderado

```
Dimensión                        Peso    Score   Peso×Score
────────────────────────────────────────────────────────────
1. SoC / SRP                     10%     2.5     0.250
2. Cohesión / Acoplamiento       10%     3.0     0.300
3. Capas / Límites               10%     2.5     0.250
4. Seguridad                     15%     1.5     0.225
5. Robustez / Resiliencia        15%     2.0     0.300
6. Consistencia transaccional    10%     2.0     0.200
7. Observabilidad                10%     1.5     0.150
8. Configuración / Entornos       5%     3.5     0.175
9. Pruebas / Calidad             10%     3.0     0.300
10. Escalabilidad                 5%     4.0     0.200
────────────────────────────────────────────────────────────
TOTAL                          100%             2.350 / 100
SCORE FINAL                                     2.35 / 5.00
```

**Interpretación:** 🔴 < 3.2: Alto riesgo — acción inmediata requerida, considerar bloquear despliegue.

El score se ve penalizado fuertemente por dos hallazgos críticos (BD y cookies en git), la falta de observabilidad estructurada, y la acumulación de deuda técnica en archivos monolíticos y ~100+ except genéricos.

---

## 4. Hallazgos

### 🔴 Críticos

| ID | Título | Dimensión | Evidencia | Riesgo | Recomendación |
|----|--------|-----------|-----------|--------|---------------|
| H-001 | BD completa + cookies de sesión trackeados en git | 4. Seguridad | `git ls-files` confirma inventario.db (475KB) y cookies.txt están en el índice de git. Historial de commits confirma que llevan meses trackeados | Exposición de todos los datos: 6 usuarios + password hashes, 65 productos, clientes (PII), movimientos, audit logs. Las cookies de sesión permiten suplantación | 1. `git rm --cached inventario.db cookies.txt` YA. 2. Rotar SESSION_SECRET. 3. Forzar cambio de contraseña a todos los usuarios. 4. `git filter-repo` para purgar del historial |
| H-001a | **FKs NO se enforcement en SQLite** — `business_delete` es dead code que corrompe datos | 6. Transaccional | `app/db.py:17-26`: no hay `PRAGMA foreign_keys=ON`. `app/routers/ui_businesses.py:205-224`: el `IntegrityError` NUNCA se lanza en SQLite | **El nuevo feature de eliminar negocios corrompe la integridad referencial silenciosamente en SQLite.** Al eliminar un negocio con productos/usuarios, la BD queda con filas huérfanas (FKs rotas). Solo Postgres está protegido | 1. Añadir `@event.listens_for(Engine, "connect")` con `PRAGMA foreign_keys=ON` en db.py. 2. En business_delete, verificar explícitamente dependencias (products, users, locations) antes de eliminar, sin depender del IntegrityError |
| H-002 | Sin protección CSRF en ningún formulario | 4. Seguridad | app/templates/ — 0 ocurrencias de csrf. FastAPI `/docs` y `/openapi.json` expuestos sin auth (`app/main.py:48` sin `docs_url=None`) | Cualquier sitio malicioso puede forzar submits cross-site. API surface expuesta públicamente. Default credentials admin/admin documentados en README:98 | 1. Validar header `HX-Request` en middleware para bloquear POSTs cross-site. 2. `docs_url=None` en producción. 3. Cambiar default de ADMIN_PASSWORD a requerir env var |

### 🟠 Altos

| ID | Título | Dimensión | Evidencia | Riesgo | Recomendación |
|----|--------|-----------|-----------|--------|---------------|
| H-003 | ~100+ bloques `except Exception` que tragan errores silenciosamente | 5. Robustez | invoice_parsers.py (~30), ui_transfers.py (~15), ui_sales_documents.py (~15), business_config.py (~10), ui_tabs.py (~15), inventory_service.py (~18), y más | Fallos en producción son invisibles. Un PDF malformado, un cálculo de FIFO roto, o una venta fallida no dejan rastro | Categorizar excepciones: IntegrityError, ValueError, HTTPException. Todo lo demás loguearlo con traceback antes de relanzar o degradar |
| H-004 | `audit.py:log_event` traga silenciosamente todos los fallos de auditoría | 5. Robustez | `app/audit.py:30`: `except Exception: db.rollback()` — no log, no raise | Si la tabla de auditoría falla (disco lleno, lock), las acciones críticas se ejecutan igual pero sin registro | Loguear el fallo al menos por stderr. Considerar escritura asíncrona o buffer en archivo como fallback |
| H-005 | 8 sentencias `print()` debug en producción | 7. Observabilidad | `app/security.py:51,62,69,80,83` y `app/routers/ui_tabs.py:1723` con `[DEBUG]` | En producción van a journald sin contexto, sin niveles de severidad, timestamp ni módulo | Reemplazar con `logging.getLogger(__name__).debug()` y configurar niveles por entorno |
| H-006 | Health check superficial sin verificación de dependencias | 7. Observabilidad | `app/routers/health.py:10`: `return {"status": "ok"}` — no verifica DB | Load balancer dirige tráfico a instancia con DB caída → 500 en todas las requests | Añadir `db.execute(select(1))` en el health check |
| H-007 | TOCTOU race en `business_create`: código duplicado → 500 | 5. Robustez | `ui_businesses.py:36-49` pre-check + `:51-62` insert sin try/except. Dos POSTs concurrentes con mismo código = IntegrityError no manejado | El nuevo feature explota con 500 si dos admins crean el mismo código simultáneamente | Envolver `db.commit()` en try/except IntegrityError y devolver 409 amigable |
| H-007a | Race condition en consumo FIFO → oversell | 5. Robustez | `inventory_service.py:2671-2701`: read-check-write sin `SELECT FOR UPDATE` ni retry | Dos ventas concurrentes del mismo producto pueden sobre-vender el stock | Añadir `with_for_update()` en la query de lots o usar SERIALIZABLE isolation |
| H-008 | Nuevo feature sin tests | 9. Pruebas | `ui_businesses.py` — 0 tests | Regresiones silenciosas en CRUD de negocios | Añadir tests: crear OK, duplicado 409, eliminar último 400, eliminar con datos, editar OK, editar duplicado 409 |
| H-008a | Authorization gaps: operadores pueden editar/eliminar ventas y crear gastos | 4. Seguridad | `ui_sales.py:371,441` sin `ensure_admin_or_owner`; `ui_expenses.py:28` sin check de rol | Un operador puede modificar o eliminar ventas (impacto financiero) y crear gastos operativos | Añadir `ensure_admin_or_owner` en sale_update, sale_delete, expense_create, expense_update |
| H-009 | Sesiones rotas en multi-instancia sin SESSION_SECRET fijo | 10. Escalabilidad | `app/utils.py:12-15`: `secrets.token_hex(32)` por proceso. Sin env var, cada worker/restart invalida cookies | Con múltiples workers o tras restart, usuarios pierden la sesión | Forzar SESSION_SECRET como requerido en producción; crash si no está configurado |

### 🟡 Medios

| ID | Título | Dimensión | Evidencia | Riesgo | Recomendación |
|----|--------|-----------|-----------|--------|---------------|
| H-010 | `ui_tabs.py` (2417 líneas) y `inventory_service.py` (3069 líneas) son monolíticos | 1. SoC | `ui_tabs.py` maneja 15+ tabs con queries DB, reportes, y renderizado. `inventory_service.py` mezcla compras, ventas, transferencias, ajustes, reportes, dividendos, y FIFO | Cambiar un reporte requiere tocar el mismo archivo que la lógica de ventas. Merge conflicts. Imposible testear unitariamente | Partir `inventory_service.py` en: `purchase_service`, `sale_service`, `transfer_service`, `report_service`. Partir `ui_tabs.py` por grupos de tabs |
| H-011 | Routers llaman DB directamente sin capa de servicio | 3. Capas | 16 queries SQLAlchemy directas en `ui_tabs.py`; múltiples en `ui_customers.py`, `ui_purchases.py`, `ui_sales.py`, `ui_businesses.py` | Lógica de query dispersa en routers — duplicación, inconsistencia, sin test unitario | Mover queries a servicios/repositorios. Crear `BusinessService` |
| H-012 | `inventory_service.py` importa y lanza `HTTPException` | 2. Acoplamiento | `from fastapi import HTTPException` en capa de servicio | Dominio acoplado al framework web. No reusable en CLI, background job, o test sin HTTP | Crear excepciones de dominio (`InsufficientStockError`, `DuplicateLotError`) y mapearlas a HTTP en routers |
| H-013 | XSS vía string interpolation en HTMLResponse | 4. Seguridad | `ui_tabs.py:2033-2039` construye `<select>` con `lot_code`/`unit_cost` sin escape; `ui_products.py:594-597`, `ui_sales_documents.py:517` interpolan valores DB sin `html.escape` | Stored XSS si un código de lote contiene HTML/JS. Solo admin/owner pueden crear lotes, pero el impacto es robo de sesión | Usar `html.escape()` o templates Jinja2 en vez de `HTMLResponse(content=...)` con f-strings |
| H-014 | Sin rate limiting ni lockout en login | 4. Seguridad | `ui_auth.py:27-62` — sin throttling. Default creds admin/admin documentados en README | Brute-force trivial con credenciales por defecto | Añadir rate limiting (slowapi o similar) en POST /ui/login |
| H-015 | Sin CI/CD pipeline | 9. Pruebas | No existe `.github/workflows/` | Sin ejecución automática de tests. Un push puede romper la build sin detección | Crear workflow GitHub Actions: pytest + lint en push a develop |
| H-016 | `business_config.conf` path hardcodeado y descubrimiento frágil | 8. Configuración | `business_config.py:90` default CWD-relativo; `migrations.py:394` glob hardcodeado `"app/business_config.*.conf"` | Si se ejecuta desde otro directorio, no encuentra la config. Negocios UI sin archivo .conf → pierden config al reiniciar | Usar `Path(__file__).resolve().parent` para rutas absolutas. Guardar config defaults en DB |
| H-017 | `NameError` en `purchase_edit_form` — bug functional | 7. Observabilidad | `ui_purchases.py:537`: referencia `currency_code` no definida en `purchase_edit_form` (solo en `purchase_update:555`) | 500 al abrir el modal de edición de compra. Stack trace leakage en dev | Definir `currency_code` antes de usarla o leerla de la config del negocio |
| H-018 | Negocios nuevos sin archivo `.conf` pierden personalización | 8. Configuración | `ui_businesses.py` no genera `.conf`. `business_config.py` busca archivo por código — si no existe, usa defaults | Negocio creado por UI funciona con defaults; al reiniciar no tiene locations definidas en config (no afecta porque locations están en DB) | Bajo riesgo inmediato. A futuro: generar `.conf` mínimo desde el UI o migrar config a DB |
| H-019 | Uploads sin límite de tamaño | 4. Seguridad | `ui_common.py:40-61`: sin max size. `ui_purchases.py:51`, `ui_products.py:438`: sin límite en PDF | DoS por disco lleno; SVGs maliciosos servidos desde /static/uploads/ | Añadir validación de tamaño y magic bytes |

### 🟢 Bajos

| ID | Título | Dimensión | Evidencia | Riesgo | Recomendación |
|----|--------|-----------|-----------|--------|---------------|
| H-016 | Patrones duplicados en repositorios | 2. Cohesión | `_normalize_text` y `_query_match` duplicados en `product_repository.py` y `inventory_repository.py` | Si cambia la lógica de normalización, hay que actualizar en 2 lugares | Extraer a `app/utils.py` o un módulo `app/query_utils.py` |
| H-017 | `Location.code` tiene `unique=True` pero varias ubicaciones comparten código | 6. Transaccional | `app/models.py:66`: `code: Mapped[str] = mapped_column(String(32), unique=True)` — la DB existente lo evita usando códigos prefijados (ROPA_CENTRAL, BAZAR_CENTRAL) | El constraint unique debería ser `(code, business_id)` compuesto. Si alguien inserta "CENTRAL" dos veces, falla | Cambiar a `UniqueConstraint("code", "business_id")` en el modelo |
| H-018 | `datetime.utcnow()` deprecado puede estar en uso | 8. Configuración | `app/utils.py:13-23`: usa `datetime.now(timezone.utc)` correctamente pero `month_range` manipula timezone-aware | Riesgo bajo — parece estar bien manejado. Solo verificar que no haya `utcnow()` residual | `grep -r "utcnow"` y reemplazar si existe |

---

## 5. Matriz impacto vs esfuerzo

```
Alto impacto │  🔥 QUICK WINS                    🎯 ESTRATÉGICOS
             │  H-001: BD/cookies en git (30m)   H-003: ~100 except Exception (4-8h)
             │  H-005: debug prints → logging    H-009: Partir inventory_service (3-5d)
             │  H-006: health check profundo     H-002: CSRF protection (4-6h)
             │  H-007: timeout absoluto sesión   H-010: Capa de servicio faltante (2-3d)
             │  H-014: https_only default=1      H-011: Excepciones de dominio (2-3d)
             │  H-008: tests feature negocios    H-012: CI/CD pipeline (2-4h)
             │                                   H-013: Config robusta para UI neg. (3-5h)
             │
Bajo impacto │  📋 BAJA PRIORIDAD                ❌ EVITAR
             │  H-016: unificar _normalize_text  H-015: Paginación server-side (sin
             │  H-017: unique composite en        datos masivos aún, postergar)
             │         Location                  H-018: Verificar utcnow residual
             └──────────────────────────────────────────────────────────────
               Bajo esfuerzo                       Alto esfuerzo
```

**Quick wins recomendados (hacer YA):**
1. **H-001a**: Añadir `PRAGMA foreign_keys=ON` en db.py + chequeo explícito en business_delete — 20 min
2. **H-001**: `git rm --cached inventario.db cookies.txt` + rotar SESSION_SECRET — 30 min
3. **H-005**: Reemplazar `print()` por `logging.getLogger()` — 20 min
4. **H-006**: Health check con verificación de DB — 15 min
5. **H-008a**: Añadir `ensure_admin_or_owner` en sale_update, sale_delete, expense_create — 5 min
6. **H-004**: Añadir `print(traceback)` al except de audit.py — 5 min
7. **H-007**: Envolver `business_create` commit en try/except IntegrityError — 10 min
8. **H-008**: Añadir tests unitarios para ui_businesses.py — 30 min

---

## 6. Backlog priorizado de remediación

| # | Hallazgo | Severidad | Esfuerzo | Impacto | Prioridad |
|---|----------|-----------|----------|---------|-----------|
| 1 | H-001: BD/cookies trackeados en git | Crítico | B | A | 🔴 Inmediata |
| 2 | H-002: Sin protección CSRF | Crítico | M | A | 🔴 Este sprint |
| 3 | H-007: Timeout absoluto de sesión | Alto | B | A | 🟠 Este sprint |
| 4 | H-005: Debug prints → logging | Alto | B | M | 🟠 Este sprint |
| 5 | H-004: Audit no silencioso | Alto | B | A | 🟠 Este sprint |
| 6 | H-006: Health check profundo | Alto | B | M | 🟠 Este sprint |
| 7 | H-014: HTTPS only default | Alto | B | A | 🟠 Este sprint |
| 8 | H-008: Tests feature negocios | Alto | B | M | 🟠 Este sprint |
| 9 | H-003: ~100 except Exception | Alto | A | A | 🟡 Próximo sprint |
| 10 | H-009: Partir inventory_service | Medio | A | A | 🟡 Próximo sprint |
| 11 | H-010: Capa de servicio routers | Medio | A | M | 🟡 Próximo sprint |
| 12 | H-011: Excepciones de dominio | Medio | A | M | 🟡 Próximo sprint |
| 13 | H-012: CI/CD pipeline | Medio | M | M | 🟡 Próximo sprint |
| 14 | H-013: Config robusta UI negocios | Medio | M | M | 🟢 Backlog |
| 15 | H-016: Unificar normalize_text | Bajo | B | B | 🟢 Backlog |
| 16 | H-017: Unique composite Location | Bajo | B | M | 🟢 Backlog |
| 17 | H-015: Paginación server-side | Medio | A | B | 🟢 Backlog |

---

## 7. Plan 30/60/90 días

### 30 días (corto plazo) — Detener la hemorragia
- [x] ~~Feature negocios implementada~~ → completado
- [ ] H-001: `git rm --cached` inventario.db + cookies.txt → **YA**
- [ ] H-001: Rotar SESSION_SECRET, invalidar sesiones existentes → **YA**
- [ ] H-005: Migrar debug prints a logging module → menos ruido en logs
- [ ] H-004: Audit failures visibles (stderr mínimo) → audit confiable
- [ ] H-006: Health check con DB verification → load balancer fiable
- [ ] H-014: `SESSION_HTTPS_ONLY` default `"1"` → cookies seguras
- [ ] H-008: Tests unitarios para ui_businesses.py → feature cubierta
- [ ] H-002: Evaluar CSRF (validar header `HX-Request` en middleware) → protección básica
- [ ] H-007: Timeout absoluto de 8h en sesiones → sesiones no eternas
**Criterio de cierre:** H-001 resuelto + score >= 3.2

### 60 días (medio plazo) — Pagar deuda técnica crítica
- [ ] H-003: Categorizar top 20 except Exception más riesgosos → visibilidad de fallos
- [ ] H-009: Extraer `report_service.py`, `dividend_service.py` de inventory_service → SRP
- [ ] H-010: Mover queries DB de ui_tabs.py a servicios → capa limpia
- [ ] H-012: GitHub Actions: pytest en push a develop → CI básico
- [ ] H-013: Config via DB para negocios creados por UI → no perder config al reiniciar
**Criterio de cierre:** score >= 3.8, ningún archivo > 1500 líneas

### 90 días (largo plazo) — Madurar la plataforma
- [ ] H-003: Eliminar todos los except Exception restantes → robustez completa
- [ ] H-011: Crear jerarquía de excepciones de dominio → desacoplar de HTTP
- [ ] H-015: Paginación server-side en tabs de historial → escalar con datos reales
- [ ] H-017: Migración de schema: UniqueConstraint compuesto en Location
- [ ] Structured logging (structlog) con correlation_id por request
- [ ] Métricas Prometheus básicas (request count, latency, error rate)
- [ ] Rate limiting en endpoints de login y API
**Criterio de cierre:** score >= 4.2, CI/CD con auditoría automática pre-merge

---

## 8. Evaluación específica del nuevo feature (ui_businesses.py)

### Lo que está bien ✅
- Sigue exactamente el patrón existente de `ui_users.py` (consistencia)
- `ensure_admin` en todos los endpoints (seguridad correcta)
- Validación de campos requeridos (code, name)
- Código único verificado antes de crear/actualizar
- `IntegrityError` capturado en delete para FK constraints
- Protección contra eliminar el último negocio
- Auto-creación de ubicación CENTRAL con código prefijado
- `log_event` para auditoría en todas las operaciones
- `X-Modal-Keep` para errores de validación en modal

### Lo que falta / mejorar ⚠️
- **Sin tests** — riesgo de regresión (H-008)
- **Sin capa de servicio** — queries DB directas en el router (H-010, consistente con el patrón actual pero es deuda)
- **No crea archivo `.conf`** — el negocio usará defaults; si se reinicia la app, `_sync_all_locations_from_config()` no hará nada porque no hay `.conf` (OK mientras no haya locations definidas en config)
- **`db.flush()` para obtener ID** — funciona pero es frágil si la sesión tiene cambios pendientes

---

## 9. Criterios de cierre

- [ ] H-001: inventario.db y cookies.txt fuera del índice de git
- [ ] H-008: Tests para ui_businesses.py (mínimo 5 tests)
- [ ] H-005: Sin print() debug en app/
- [ ] Score de re-auditoría >= 3.5

---

*Informe generado por Claude Code (skill: auditoria-ingenieria).*
*Re-auditoría recomendada: 2026-09-04 (o después de completar el plan 30 días).*
